# Maximum Subarray Given an integer array nums, find the subarray with the largestsum, and return its sum.
#Input : nums = [-2,1,-3,4,-1,2,1,-5,4]
#Output: 6
#Explanation: The subarray [4,-1,2,1] has the largest sum 6.
#Input: nums = [1]
#Output: 1
#Explanation: The subarray [1] has the largest sum of 1.


# Here we know the list of integers. from this we need to find the max_subarray. here, inintially we take max_subarray as zero.
# we use two pointer for this problem. because we have to find the range of the indexs.
class Solution:
    def max_subarray(self,nums):
        max_sum = 0
        current_sum = 0
        for i in range(len(nums)):
            num = nums[i]
            current_sum = max(num, current_sum + num) # 1, 3
            max_sum = max(max_sum, current_sum) # 1, 3
        return max_sum
        
if __name__ == "__main__":

    max_sub_cls = Solution()
    nums = [1,2,4 -1, 3, 2, -4]
    # print(max_subarray)
    result = max_sub_cls.max_subarray(nums)
    print(result)